# Dependencies (package-lock.json IS committed — CI installs from it with npm ci,
# so test runs pin the exact jsdom version instead of floating ^24)
node_modules/

# Secrets — never commit these (see CLAUDE.md). Auth uses runtime MSAL tokens only.
.env
.env.*
*.key
*.pem
secrets.json
appsettings*.json

# OS / editor cruft
.DS_Store
Thumbs.db
Desktop.ini
*.swp
*~
.vscode/
.idea/

# Logs / temp
*.log
npm-debug.log*
tmp/
