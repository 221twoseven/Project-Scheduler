# Handoff — 2026-08-20 (end of day)

**For whoever picks this up next** (person or session). World-state as of this commit;
the standing rule applies — re-verify before quoting, `TODO.md` is the live list.
This lives in the repo deliberately: the 2026-08-17 handoff travelled outside it and
went missing for three days.

## Where things stand in one paragraph

REV57 shipped today: all nine ungated items from the recovered field notes (TODO §3a)
— the full project-page refinement track. It is pushed to `development`, deployed to
`/preview/`, tested 20/20 suites on both the live build and the frozen REV50 reference,
and **[PR #15](https://github.com/221twoseven/Project-Scheduler/pull/15) is open to
promote REV53–57 to `main`**. The owner merges it (ruleset bypass or after review) —
nothing else blocks it but that click. Hallway test round 2 is planned for **tomorrow,
2026-08-21**, on `/preview/` — the page testers will live on is finished.

## What shipped today (REV57, eight commits `7324454..29bfb12`)

Details and ceilings: `docs/Milestones/2026-08-20-project-page-refinement.md`.
Short version: status labels lose the "In" (N5), the orphan row is "Events" (N8), the
main-timeline toolbar mutes on the project page (N15), the status pill anchors in the
schedule footer (N4), a breadcrumb tail follows selection (N1), dirty unsaved drafts
ask before discard (N2), department rows get bidirectional workday-snapped Start/End
date fields (N13), left-click edits / right-click adds with inline-name create menus
(N11 — Design-Language §6 updated), and the calendar collapses roster fan-out with a
"Selected only" filter (N16).

New suite: `tests/test57.js` (35 assertions, skips pre-REV57). `test48/49/50` gate
their old menu-shape assertions behind an N11 feature sniff so `npm run test:ref`
still guards the frozen reference; `test53` updated in place (never runs on the
reference). Evidence PNGs: `docs/Milestones/screenshots/rev57-*.png`.

## The critical path from here

1. **Owner conversation — one sitting unblocks everything gated.** ⚠ `email` + `role`
   columns on `ShopTimeline_Staff` (unblocks Dash view, §3 item 4); ⚠ new
   `ShopTimeline_Clients` list (unblocks N3); N9 tasks-vs-events (unblocks the N6/N7
   agenda editors); client-color's role (recommended: it becomes the project hue);
   and the two standing decides — bar dependencies, department overlap.
2. **Hallway test round 2** (tomorrow) — five tasks from `UX-Audit-and-Strategy.md` §6,
   compare assist counts to round 1. Run it on `/preview/`.
3. **Merge PR #15** — owner's call, before or after the hallway test.
4. **Then:** Dash view (once Staff schema lands), N6/N7 agenda editors (once N9 is
   decided), N3 client list (once the list is approved).

Also still on the owner: **create the `ShopTimeline_Events` list** (approved 2026-08-19,
column recipe in `SETUP.md`) — until it exists the app uses the legacy phase-hosted
event save.

## Traps for the next person

- **The REV49 lesson still rules:** every feature asserts on BOTH the new-project
  draft page and the saved page (`tests/README.md`).
- **The reference build is immutable** (`reference/Timeline_50.html`). When a REV57+
  behavior change breaks an old assertion, gate it behind a feature sniff (the
  convention: `const N11=/\.npv-menu \.mn/.test(src)` — see test48/49/50 for the
  pattern); never edit the reference.
- **PowerShell 5.1 mangles git commit messages containing double quotes** (native-arg
  quoting); one REV57 commit got mashed that way. Use Git Bash or `--body-file`-style
  file inputs for anything multiline.
- **Duplicate DOM id `pp-cancel`** in `index.html` (print overlay's Close + project
  page's Cancel). Works today only by DOM ordering. Rename one next time that area is
  touched — don't add a third.
- **jsdom double-fires `hashchange`** vs a real browser's single fire; `applyRoute`
  now guards re-entry into the draft route (`PP_KEEP` set when `r.creating &&
  ROUTE.creating`) — keep that guard if the router changes.
- **Screenshot evidence pipeline:** stub generator pattern is documented in the
  2026-08-14/20 milestone records — headless Chrome against a `file://` stubbed copy
  (`make-preview.js` style, mirrors `tests/harness.js`); PNGs go in
  `docs/Milestones/screenshots/`, absolute long-form `--screenshot=` path or Chrome
  errors with "Access is denied".

## Deliberate ceilings added today (revisit on complaint, not before)

Listed in full in the REV57 milestone record: merged calendar bands select their
first bar; department date fields move only the primary bar; the checklist days→end
recompute doesn't change the inspector Days field's meaning; "Selected only" keeps
event/task markers; "Remove from job" now lives only in the inspector checklist.

## State of the branches

- `development` = `29bfb12` (REV57 + docs), CI + Deploy Pages green, live at
  `/preview/`.
- `main` = pre-REV53 production; PR #15 promotes.
- `sandbox` = collaborator's copy, unaffected.
