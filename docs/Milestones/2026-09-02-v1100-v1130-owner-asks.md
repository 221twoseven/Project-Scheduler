# 2026-09-02 — v1.10.0 → v1.13.0: the nine-item morning brief

Four releases in one day, all on `development`, one full suite run at the end
(69/69 both builds — the owner OK'd skipping per-bump full runs for same-day
batches). TODO §3 items 33–36 + the item-30 close-out.

## v1.10.0 — the quick batch (items 1, 2, 3, 5, 7, 8)

- **Dock re-layout:** Time off stacks UNDER Working on in one column; Milestones and
  Notes went back to their own columns; User Notes keeps the far right.
- **Comprehensive departments:** the People page's checkboxes and filter now offer the
  same full department list the project-edit phase picker uses — `SM_DEPTS` is derived
  from `DEPTS` (free-text "Other" buckets excluded), closing the REV90 ledger entry
  about the duplicated ids. **Logistics is a real department** (list, group, dept-lens
  section, team color).
- **Wordmark goes home:** clicking TWOSEVEN INC. returns to the Global Gantt from any
  route (drafts keep their discard confirm; dashboards exit cleanly).
- **Signed-in badge:** the top right shows who's logged in next to the sync pill, with
  a DEV or ADMIN chip. The chip shows the account's *real* access — it doesn't flip
  while the Viewer preview is on.
- **One tour:** the home tour's last step highlights + New Project and asks for a real
  click (the overlay lets exactly that click through; Enter presses it; everything
  else is swallowed by a capture guard). The project-page tour continues on the draft
  that opens, so nobody mistakes half the tour for the whole thing. Viewers skip the
  chain step.

## v1.11.0 — dev App Settings + granular viewer grants (item 6)

Help ▸ App settings (`#/settings`, developers only — non-devs bounce home) hosts
app-wide switches on the new app-side **`ShopTimeline_Config`** list (Title = key,
one `value` column; owner creates it — until then the page works browser-local and
says so inline). Everyone reads config at sign-in; changes reach users on their next
reload.

The viewer.* switches each open ONE edit door for non-admins — project settings,
phase edits (incl. the Lock Dates toggle back), subtasks, milestones, notes — via
`vcan(door)` at every former `isViewer()` gate, `viewerLock(box, kind)`, per-grant
body classes for CSS chrome, and self-grading agenda rows. Defaults all OFF (owner:
non-admins stay mostly gated for now). Honest ceilings in TODO §7: the saveState
choke point opens wholesale on any grant, and the right-click menu opens on the
phases grant only.

## v1.12.0 — "Listening to" (item 9; item 30's second half, revised spec)

Summary pages' dock header grows a right-justified dark-grey personal line —
`[listening to | reading | thinking about]: Title`, hyperlinked when a link is set.
Never on your own dashboard; empty title or Show off auto-hides; the whole feature is
experimental behind the `exp.listening` App-settings switch. You edit yours through a
thought-cloud icon at the dock's bottom-right — a field-sized popover that commits
once, on close, through the v1.9.0 self-row staff save. Storage: four tristate
columns on `ShopTimeline_Staff` (`listeningTo`, `listeningLink`, `listeningVerb`,
`listeningShow` — spec with Robert).

## v1.13.0 — the People page mirrors Employee Contacts (item 4)

An admin-only **Import from Employee Contacts** button reads the HR list (READ-ONLY —
never written, never schema-touched; Pay Type and PersonalEmail never even read) and
mirrors it into the roster in one confirm: active newcomers are added (Current Title
→ role, phone, work email, department mapped to an app id — unmapped strings reported,
never guessed), matched people take HR as master for title/phone/status while curated
departments and existing emails are kept, non-active rows never create people, and
nothing is auto-removed. Join key = work email with an exact-name fallback — the same
`meName()` identity chain, so after one run every M365 sign-in that exists on the HR
list pairs with a roster row. The `status` column (vocabulary = the HR Status values)
is probed live and starts syncing the run after Robert creates it. Field names
resolve `_x0020_`/case/space-insensitively, so the HR list's internal names don't
matter. Re-runs are idempotent.

**Answer to the owner's access question:** every TwoSeven M365 login can open the app
(single-tenant Entra app, admin-consented scopes). Pairing to roster data is by work
email; the import makes that pairing complete and consistent. Unmatched sign-ins are
viewers with an unresolved dashboard — the import report (console + confirm text) is
where mismatches surface.

## Owner actions

1. Create `ShopTimeline_Config` (Title + `value`, single-line text).
2. Create on `ShopTimeline_Staff`: `personalNotes` (multi-line — carried from
   v1.9.0), `listeningTo`, `listeningLink`, `listeningVerb`, `listeningShow`,
   `status` (all single-line text).
3. Run **Import from Employee Contacts** on `/preview/` (People page), review the
   confirm + console report, re-run after creating `status`.
4. Verify the tour chain and the Listening-to line on `/preview/` (toggle
   `exp.listening` on in Help ▸ App settings).

## Records

Suite: `tests/test-v1100.js` (43 checks; registered in `run.js`; test67/test74/
test-v190 branched on the new markers). Design-Language §7.6 grew the grants,
badge/wordmark, chained-tour and Listening-to rules. Commits: `fb0f983` (v1.10.0),
`f2b30ef` (v1.11.0), `4daa362` (v1.12.0), `008d146` (v1.13.0), plus the tests+docs
commit carrying this record.
