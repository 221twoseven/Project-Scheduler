# 2026-09-01 — v1.7.1: Fonts (objs 10–11) + the staff phone field

Quick-wins patch on the heels of v1.7.0, unblocked by the owner's same-day answers.

## What changed

- **Brauer Neue for the TWOSEVEN title (obj 10).** Licence confirmed by the owner;
  of the six supplied weights only **`fonts/BrNStdBd.otf`** (the bold the owner
  named) is committed and deployed — the public repo/site publishes only what the
  app uses; the other weights stay local. `@font-face 'Brauer Neue'` + a
  `font-family` on `.tb-co`. The file joined all three sparse-checkout lists in
  `deploy-pages.yml`; **until that workflow reaches `main`/`sandbox`, a push to
  either fails the deploy at the guard step** (non-destructive — ledgered, TODO §7).
- **Bahnschrift app-wide (obj 11), the no-file rung.** `--sans` now leads with
  `Bahnschrift` — resolved via `local()` on every Windows 10+ machine, nothing
  committed; non-Windows falls through to the previous stack unchanged. Verified
  legible at the timeline's smallest sizes in a real browser.
- **`phone` joins the person record.** The owner created `admin`,
  `feedbackRecipient`, and `phone` columns on `ShopTimeline_Staff` today; the app
  now round-trips `phone` (field mappers), shows it read-first on the People page,
  and edits it in the record editor. `admin`/`feedbackRecipient` wait for the
  v1.8.0 permissions batch.

## Verification

`tests/test-v171.js` (10 checks): font-face/stack/allowlist source assertions, the
phone field-mapper round trip, People-page read/edit, and the outgoing PATCH body.
Full suite green on both builds before push.

## Known ceilings / follow-ups

- The stored column's internal name must be exactly `phone` — a mismatch would 400
  staff saves once a phone is entered (owner asked to verify; TODO §5).
- Committed Bahnschrift TTF still gated on non-Windows coverage demand (TODO §7).
